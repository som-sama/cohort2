//write a program to greet a person given their first and last name

export function greeting(first_name, last_name){
    return `${first_name} ${last_name}`
}

    let name = "Soumyadeep"
    let name2 = "Datta"

    console.log(greeting(name,name2))